[build]
  command = "npm run build"
  publish = "dist"

# A single-page app: every path has to reach index.html, or a refresh on a
# deep link 404s.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
